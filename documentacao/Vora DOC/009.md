# 📓 Registro Vora DOC 009 - 20/07/2026

**Versões Correspondentes:** 
* Versão 2.3 - Controle de Assinaturas, Bloqueio de Período de Teste (2 Dias), Suporte WhatsApp e Design iOS 18 Glassmorphism (AUTOLIST - MAVC)

## 🎯 Objetivo da Sessão
1. Implementar o sistema de controle de assinatura e aplicação de prazo rígido de teste de **2 dias grátis** (`test_2d`) para novos lojistas.
2. Bloquear o acesso ao painel do lojista caso a conta esteja expirada ou suspensa, direcionando o lojista para renovação via canal de suporte WhatsApp (**+55 62 99404-9949**).
3. Desenvolver o componente `SubscriptionLockModal.jsx` com padrão estético **iOS 18 Glassmorphism Dark**, brilhos neon e suporte completo à rolagem vertical no celular (`max-h-[90vh] overflow-y-auto`).
4. Resolver o conflito de CSS em `index.css` (overrides do tema claro) injetando a classe `lock-mode` no `body` durante o bloqueio, garantindo contraste máximo com textos em branco puro (`#FFFFFF`) e acentos esmeralda.
5. Adicionar usuário e atalho de teste rápido (`expirado@autolist.com` / `🔴 Ver Expirado`) para validação instantânea.

## 🛠️ Alterações Efetuadas
* **[NEW]** `src/components/SubscriptionLockModal.jsx`: Componente de bloqueio de tela por expiração/suspensão de assinatura com design iOS 18 Glassmorphism, card de identificação da loja, suporte com link direto para WhatsApp e botão de encerramento de sessão.
* **[MODIFY]** `src/services/authService.js`:
  * Atualizada a duração padrão de teste de `test_3d` para `test_2d` (2 dias / 48 horas).
  * Adicionados métodos de verificação `authService.isExpired(user)` e `authService.isLocked(user)`.
  * Adicionada conta de demonstração expirada `EXPIRED_LOJISTA` (`expirado@autolist.com` / `123456`).
* **[MODIFY]** `src/components/Layout.jsx`: Injetado o gatekeeper `SubscriptionLockModal` quando `currentUser` estiver com acesso bloqueado.
* **[MODIFY]** `src/components/AuthModal.jsx`: Adicionado o botão de atalho `🔴 Ver Expirado` para acionamento imediato da tela de renovação.
* **[MODIFY]** `src/components/GestorPortal.jsx`: Atualizadas todas as opções e métricas de teste no Portal do Gestor para o padrão **2 Dias (`test_2d`)**.
* **[MODIFY]** `src/index.css`: Adicionado o seletor `body.lock-mode` e isolados os overrides do painel claro (`body:not(.gestor-mode):not(.lock-mode)`), permitindo escurecimento total do fundo e textos brancos sem interferência.

## 🎨 Decisões de Design (iOS 18 Glassmorphism)
* **Visual Premium Translucido:** Utilizado `bg-zinc-950/80` com `backdrop-blur-3xl` e bordas finas de vidro `border-white/15`, alinhado à linguagem do iOS 18.
* **Iluminação Neon Fluida:** Luzes difusas em verde esmeralda (`emerald-500/20`) e ciano no fundo do modal para criar profundidade sem poluição visual.
* **Ergonomia e Rolagem Mobile:** O container foi limitado a `max-h-[90vh]` com rolagem nativa vertical (`overflow-y-auto scrollbar-thin`), impedindo que botões de suporte fiquem inacessíveis em telas pequenas de smartphones.

## 🔳 Testes e Validação
* **Build de Produção:** Executado com sucesso via `npm run build` sem erros ou divergências.
* **Sincronização Cloud:** Código enviado e versionado na branch `main` do GitHub (`manaeletro-bot/autolist-mavc`) e sincronizado com o ambiente Vercel Cloud.
