# 📓 Registro Vora DOC 008 - 20/07/2026

**Versões Correspondentes:** 
* Versão 2.1 - Otimizações de UX Mobile, Integridade de Mídia e Cabeçalho Dinâmico (AUTOLIST - MAVC)

## 🎯 Objetivo da Sessão
1. Ajustar e padronizar visualmente o Card do Veículo no Dashboard, simplificando os indicadores financeiros exibidos no card.
2. Resolver o corte de texto em dispositivos móveis nos subtítulos dos acordeões da tela de detalhes do veículo.
3. Integrar as informações do veículo (marca, placa, modelo e botão de voltar) diretamente na barra de cabeçalho fixa no mobile, elevando os botões de ação primários (`Vender`, `Baixar`, `Excluir`).
4. Sincronizar, converter e otimizar todas as fotos de veículos da pasta de teste (`CAR TESTE FOTOS`) para a estrutura pública do projeto (`public/images/`), resolvendo o problema de corte de imagens com orientação retrato.

## 🛠️ Alterações Efetuadas
* **[NEW]** `public/images/`: Sincronizadas e otimizadas todas as mídias locais dos 6 veículos de teste (`tesla`, `focus`, `hrv`, `sw4`, `byd`, `onix`) na proporção 16:9 landscape (`1280x720`).
* **[MODIFY]** `data/db.json`: Atualizados os caminhos de fotos de todos os 6 veículos para apontar para `/images/[modelo]/...`, eliminando dependências de URLs genéricas do Unsplash.
* **[MODIFY]** `src/components/Dashboard.jsx`: Redesenhado o `VehicleCard` tornando-o 100% clicável, exibindo marca, modelo, ano, placa, badge de cor e unicamente o Preço de Revenda.
* **[MODIFY]** `src/components/VehicleDetails.jsx`: 
  * Substituído a classe `truncate` por `break-words leading-tight` nos 6 subtítulos das seções sanfona.
  * Ocultado o bloco de título no mobile quando os detalhes estão ativos (pois agora é exibido no topo do app).
  * Reorganizados os botões de ação (`Vender`, `Baixar`, `Excluir`) para ocuparem a linha superior inicial.
* **[MODIFY]** `src/components/Layout.jsx` & `src/App.jsx`:
  * Adicionado suporte às props `selectedVehicle` e `onBackVehicle` no `Layout`.
  * Renderizada a identificação do veículo e botão de voltar na barra de cabeçalho fixa mobile durante a navegação interna.

## 🎨 Decisões de Layout e Mídia
* **Navegação Mobile Native-like:** Ao mover o título e botão de voltar para o header fixo, o app ganha um aspecto de aplicativo nativo e libera mais espaço útil vertical no celular.
* **Proporção 16:9 em Mídia:** Mídias com orientação vertical/retrato (960x1280) foram convertidas para 16:9 com padding desfocado de fundo, evitando que o `object-cover` corte a frente e a traseira dos carros.

## 🔳 Testes e Validação
* **Build de Produção:** Executado com sucesso via `npm run build` sem erros de compilação ou lint.
* **Persistência Git:** Alterações consolidadas e enviadas com sucesso para a branch `main` no repositório GitHub (`manaeletro-bot/autolist-mavc`).
