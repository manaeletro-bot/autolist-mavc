# 📓 Registro Vora DOC 010 - 20/07/2026

**Versões Correspondentes:** 
* Versão 2.4 - Otimização Responsiva Mobile do Portal do Gestor & Roteamento SPA na Vercel (AUTOLIST - MAVC)

## 🎯 Objetivo da Sessão
1. Otimizar a interface do **Portal do Gestor Master (`/gestor`)** para exibição perfeita em telas de smartphones/celulares.
2. Resolver o corte horizontal de tabelas e textos e aplicar um **design de Cards Responsivos** para uso em toque no celular.
3. Adicionar regras de redirecionamento SPA no `vercel.json` para que o link direto `https://autolist-mavc.vercel.app/gestor` funcione online sem erro 404.

## 🛠️ Alterações Efetuadas
* **[MODIFY]** `src/components/GestorPortal.jsx`:
  * Criada a visão **Mobile Responsive Cards (`md:hidden`)**: cada lojista é exibido em um card vertical escuro de alto contraste, com avatar, nome da loja, e-mail completo copiável, selo de status, selo de licença e botões de ação ("Alterar Licença", "Suspender/Ativar", "Excluir") com alvos de toque amigáveis.
  * Mantida a **Tabela Clássica de Desktop (`hidden md:block`)** para telas grandes.
  * Ajustada a barra de filtros superiores com rolagem suave `scrollbar-none` e selos `shrink-0 whitespace-nowrap`, evitando que os botões "TODOS", "ATIVOS", "SUSPENSOS", "TESTE 2D" e "VITALÍCIO" fiquem esmagados ou cortados no celular.
  * Modais com rolagem interna `max-h-[90vh] overflow-y-auto` para caberem em telas de smartphones de qualquer resolução.
* **[MODIFY]** `vercel.json`:
  * Adicionada regra de reescrita SPA (`"rewrites": [{ "source": "/gestor", "destination": "/index.html" }]`) garantindo acesso direto ao portal online sem erro 404.

## 🎨 Decisões de Layout e Design (UX/UI)
* Estilo dark iOS-inspired com bordas finas, cores de contraste neon (âmbar, esmeralda, roxo e vermelho) e excelente legibilidade em telas pequenas.

## 🔳 Testes e Validação Manual
* **Build de Produção:** Executado `npm run build` com sucesso.
* **Envio para GitHub & Vercel:** Commit `71e9689` enviado e implantado online.
